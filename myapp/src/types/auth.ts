export type RegisterForm = {
  username: string;
  email: string;
  password: string;
};
export type FormAuth = {
  username: string;
  email: string;
  password: string;
};
export type User = {
   id: string, 
   name: string, 
   image: string,
   password:string
}